export { loader } from '@routes/index/loader';

/**
 * This module only re-exports the loader function from the loader module in the same directory.
 * The only reason to do this is to redirect the user from / to /dashboard.
 */
